# Produzione: UI servita dal backend → richieste same-origin su /api e /hubs (come il proxy Vite in dev).
VITE_API_BASE_URL=
# "true" = nasconde i pulsanti Accedi e Registrati nella home
VITE_HIDE_AUTH_BUTTONS=true
